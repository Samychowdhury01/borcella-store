import { Loader2, type LightbulbIcon as LucideProps } from "lucide-react"
import { FcGoogle } from "react-icons/fc"

export type IconProps = typeof LucideProps

export const Icons = {
  spinner: Loader2,
  google: FcGoogle,
}

